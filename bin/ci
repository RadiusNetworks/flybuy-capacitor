#!/bin/bash
set -e

SCRIPT_DIR="$(dirname "$0")"

echo "========================================"
echo " flybuy-capacitor — Local CI"
echo "========================================"

bash "$SCRIPT_DIR/check-build"
echo ""
bash "$SCRIPT_DIR/check-ts"
echo ""
bash "$SCRIPT_DIR/check-lint"
echo ""
bash "$SCRIPT_DIR/check-audit"
echo ""
bash "$SCRIPT_DIR/check-android"
echo ""
bash "$SCRIPT_DIR/check-ios"

echo ""
echo "========================================"
echo "✅ All checks passed!"
echo "========================================"